#include <stdio.h>

//void rewind(FILE *fp);
//把文件指针移动到文件开头 
//long ftell(FILE *fp);
//返回当前文件指针距离文件开头的字节数，出错时返回-1 
//int fseek(FILE *stream, long offset, int whence);
//移动文件指针的位置，参数offset为位移量（正数往后移 负数往前移），参数whence为起始点（0文件开头 1当前位置 2文件结尾） 

int main()
{
	printf("看注释。\n");
	
	return 0;
}
